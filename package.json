{
  "name": "razer-chroma",
  "description": "Chroma for Node",
  "version": "0.1.1",
  "main": "index.js",
  "scripts": {
    "configure": "node-gyp configure",
    "build": "node-gyp build",
    "install": "npm run configure && npm run build",
    "test": "standard \"**/*.js\""
  },
  "license": "MIT",
  "repository": "https://github.com/felixrieseberg/razer-chroma",
  "os": [
    "win32"
  ],
  "dependencies": {
    "bindings": "^1.2.1"
  },
  "devDependencies": {
    "nan": "^2.5.1",
    "node-gyp": "^3.5.0",
    "standard": "^8.6.0"
  }
}
